import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-todolist',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
